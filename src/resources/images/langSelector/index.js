import enButton from "./enButton.svg";
import esButton from "./esButton.svg";
import itButton from "./itButton.svg";

export const en = enButton;
export const es = esButton;
export const it = itButton;
